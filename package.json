{
  "name": "flashr-mac",
  "productName": "Flashr",
  "version": "0.1.0",
  "description": "Flashr for Mac. RSVP speed reader, summoned from anywhere.",
  "main": "main.js",
  "author": "Posthuman",
  "license": "UNLICENSED",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . Flashr --platform=darwin --arch=arm64 --icon=build/icon.icns --no-asar --out=dist --overwrite --app-bundle-id=health.posthuman.flashr --app-version=0.1.0"
  },
  "devDependencies": {
    "@electron/packager": "^20.0.1",
    "electron": "^42.4.0"
  },
  "dependencies": {
    "adm-zip": "^0.5.17",
    "pdf-parse": "^2.4.5"
  }
}
